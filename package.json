{
	"name": "battleship-monorepo",
	"version": "0.0.1",
	"description": "Monorepo for Battleship API and UI",
	"author": "Evan Ristow",
	"private": true,
	"license": "UNLICENSED",
	"scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --filter=./apps/* build",
    "test": "pnpm --parallel test",
    "lint": "pnpm --parallel lint",
    "clean": "rimraf **/node_modules **/dist"
	},
	"devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}